wrap_args=()

# Example: unsafe shared directories.
# wrap_args=(
#     --bind ~/.cargo ~/.cargo
#     --bind ~/.rustup ~/.rustup
#     --bind ~/.ivy2 ~/.ivy2
#     --bind ~/.m2 ~/.m2
#     --bind ~/.coursier ~/.coursier
# )
